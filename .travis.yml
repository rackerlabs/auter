language: bash
services:
    - docker
before_install:
    - docker pull centos
script:
    - "tests/01-spellcheck.sh"
    - "tests/05-shellcheck.sh"
    - "docker --version"
    - "sudo docker run --rm=true --mount type=bind,source=$(pwd)/tests,destination=/tests --name auter-test -td centos:6"
