language: bash
services:
    - docker
before_install:
    - docker pull centos
    - docker run -d -p 127.0.0.1:80:4567 centos /bin/sh -c "ls -l /root/"
script:
    - "echo 'Testing'"
    - "sh ./test_auter.sh"
    - "sestatus"
