language: bash
services:
    - docker
before_install:
    - docker pull centos
script:
    - "echo 'Testing'"
    - "docker run --name test -td centos"
    - "docker exec test yum -y install git"
    - "ls -lah"
    - "ls -lah auter*"
