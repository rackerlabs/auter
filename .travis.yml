language: bash
services:
    - docker
before_install:
    - docker pull centos
script:
    - "shellcheck auter"
    - "docker --version"
    - "sudo docker run --rm=true --mount type=bind,source=`pwd`/tests,destination=/tests --name test -td centos:6"
    - "ls -lah"
    - "ls -lah auter*"
